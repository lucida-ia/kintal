import type { Config } from "tailwindcss";

const config: Config = {
  // In Tailwind v4, content paths are automatically detected from your project structure
  // or can be defined in your CSS file using @source
  plugins: [],
};

export default config;
